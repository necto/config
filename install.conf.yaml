- defaults:
   link:
     reling: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.spacemacs: .spacemacs
    ~/.vimrc: .vimrc
    ~/.bashrc: .bashrc
    ~/.gitconfig: .gitconfig
    ~/.xmonad: xmonad

- create:
    - ~/proj

- shell:
  -
    command: sh emerge-spacemacs.sh
    description: Downloading Spacemacs
    stdout: true
    stderr: true
    stdin: true
