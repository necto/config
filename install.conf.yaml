- defaults:
   link:
     reling: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.spacemacs: .spacemacs
    ~/.vimrc: .vimrc
    ~/.bashrc: .bashrc
    ~/.gitconfig: .gitconfig

- shell:
  - [sh emerge-spacemacs.sh, Downloading Spacemacs]
